<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Theme.ToepScoreTracker" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary brand color — used for filled button backgrounds -->
        <item name="colorPrimary">@color/accent_green</item>
        <item name="colorPrimaryContainer">@color/primary_dark_green</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorOnPrimaryContainer">@color/white</item>
        <!-- Secondary brand color -->
        <item name="colorSecondary">@color/accent_green</item>
        <item name="colorSecondaryContainer">@color/primary_dark_green</item>
        <item name="colorOnSecondary">@color/white</item>
        <item name="colorOnSecondaryContainer">@color/white</item>
        <!-- Surface — cards and surfaces use the table green -->
        <item name="colorSurface">@color/primary_green</item>
        <item name="colorOnSurface">@color/white</item>
        <!-- Outline color for outlined buttons and text fields -->
        <item name="colorOutline">@color/outline_white</item>

        <!-- Window and text -->
        <item name="android:windowBackground">@color/primary_green</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:editTextColor">@color/white</item>
        <item name="android:textColorHint">#BDBDBD</item>

        <!-- Status bar -->
        <item name="android:statusBarColor">@color/primary_dark_green</item>
    </style>
</resources>
